import React from 'react';
import { Layout } from 'antd';

const { Content } = Layout;

const ChatContent = () => {
  return <Content className="chat-content"></Content>;
};

export default ChatContent;
